# UI 同步問題修復報告

## 發現的問題

### 問題 1: 「僅顯示需補貨」篩選會改變儀表板數據

**重現步驟:**
1. 在 index.html 匯入 shopee_products.json
2. 儀表板顯示全部商品統計（例如：補貨53, 銷量11828, 庫存32637）
3. 啟用「僅顯示需補貨型號」checkbox
4. 儀表板數據改變（銷量→10189, 庫存→24785）

**預期行為:**
- 儀表板應該始終顯示「全部商品」的統計數據
- 篩選條件（僅顯示需補貨）只應該影響表格顯示，不應該影響儀表板

**實際行為:**
- 儀表板數據隨著篩選條件而變化
- 啟用篩選後，不需補貨的型號被排除在統計之外

**根本原因:**
在 `script.js` 的 `calculateInventoryStatistics` 函式中（第 1955-1962 行），當 `filterMode` 為 true 時，函式會跳過不需補貨的型號：

```javascript
if (filterMode) {
    if (expectedStock > 0 && currentStock >= expectedStock) {
        return;  // 跳過此型號，不計入統計
    }
    if (expectedStock === 0 && currentStock > 0) {
        return;  // 跳過此型號，不計入統計
    }
}
```

這導致 `totalStock` 和 `totalMonthlySales` 只計算需補貨的型號，而不是全部型號。

### 問題 2: 進階搜尋也會改變儀表板數據

**重現步驟:**
1. 匯入商品後，儀表板顯示全部統計
2. 使用進階搜尋過濾商品（例如搜尋「白」）
3. 儀表板數據改變，只顯示搜尋結果的統計

**預期行為:**
- 進階搜尋應該只過濾表格顯示
- 儀表板應該保持顯示全部商品的統計
- 或者明確告知使用者儀表板顯示的是「過濾後」的統計

**實際行為:**
- 儀表板數據改變，但沒有明確指示這是過濾後的統計
- 可能造成使用者混淆

**根本原因:**
同樣在 `calculateInventoryStatistics` 函式中（第 1882-1912 行），函式會根據進階搜尋關鍵字過濾商品，然後只計算過濾後商品的統計。

## 修復方案

### 方案 A: 儀表板始終顯示全部資料統計（推薦）

**修改 `calculateInventoryStatistics` 函式:**
1. 移除第 1955-1962 行的 `filterMode` 判斷邏輯
2. 儀表板始終計算全部商品和型號的統計
3. `filterMode` 和進階搜尋只影響表格顯示

**優點:**
- 儀表板提供全局視圖，不受篩選影響
- 使用者可以同時看到整體統計和篩選後的表格
- 符合一般 UI/UX 慣例（儀表板顯示總覽，表格可篩選）

**缺點:**
- 需要確保表格的篩選邏輯獨立運作

### 方案 B: 儀表板顯示過濾後統計，但加上明確指示

**修改方式:**
1. 保持現有邏輯
2. 在儀表板上加上明確的文字說明，例如：
   - 「全部商品」vs「僅需補貨」
   - 「全部」vs「搜尋結果：白」

**優點:**
- 保持彈性，使用者可以看到過濾後的統計
- 程式碼改動較小

**缺點:**
- UI 較複雜
- 可能仍然造成混淆

## 測試結果

**測試檔案:** `test_ui_bugs.py`, `test_comprehensive.py`

**測試涵蓋:**
- ✅ 匯入商品後儀表板和表格同步
- ✅ 更改庫存月份後同步
- 🐛 僅顯示需補貨篩選的同步問題（已發現）
- ✅ 進階搜尋與清除的同步
- ✅ 頁面導航後資料狀態

**測試資料:**
- `shopee_products.json`: 1 個商品，1 個型號
- `shopee_products_large.json`: 10 個商品，82 個型號

## 建議修復

建議採用**方案 A**，原因：
1. 符合 UI/UX 最佳實踐
2. 避免使用者混淆
3. 儀表板提供穩定的全局視圖

## 修復檔案

- `script.js`: 修改 `calculateInventoryStatistics` 函式
